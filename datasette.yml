databases:
  secrets:
    allow:
      id: root

permissions:
  manage-secrets:
    id: root

plugins:
  datasette-secrets:
    encryption-key:
      $file: /workspaces/studio/.datasette-secrets-key
    database: secrets
